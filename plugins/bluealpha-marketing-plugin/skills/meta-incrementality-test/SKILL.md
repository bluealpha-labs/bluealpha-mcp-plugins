---
name: meta-incrementality-test
description: Design and monitor geo-based incrementality / lift tests for Meta (Facebook/Instagram) with integrity checks against platform-reported ROAS. Covers geo holdout design, Meta Conversion Lift framing, power/duration, and reading results via BlueAlpha's incrementality tooling. Use when the user says "Meta incrementality test", "Meta geo test", "Meta holdout test", "is Facebook actually driving conversions", "Meta ROAS isn't real", "Conversion Lift", "prove Meta incrementality", or wants to validate that Meta delivers causal lift, not just last-touch/modeled credit.
---

Read references/workflow.md for the full workflow. Tool surface: verified `facebook_ads_*` (see ../meta-auto-optimize/references/meta-mcp-tools.md) plus the LIVE `incrementality_*` and `meridian_*` tools in the connector.
